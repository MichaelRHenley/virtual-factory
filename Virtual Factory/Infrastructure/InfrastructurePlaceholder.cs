namespace Virtual_Factory.Infrastructure
{
    // Infrastructure concerns live here: MQTT client wrappers,
    // hosted background services (subscriber, simulator publisher),
    // and helpers for loading seed/profile JSON files at startup.
    internal static class InfrastructurePlaceholder { }
}
