namespace Virtual_Factory.Endpoints
{
    // Endpoint mapping classes will be added here.
    // Each domain area (assets, points, workorders, etc.) will have its own
    // static class with extension methods on WebApplication.
    internal static class EndpointsPlaceholder { }
}
