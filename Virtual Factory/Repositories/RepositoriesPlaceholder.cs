namespace Virtual_Factory.Repositories
{
    // In-memory repository interfaces and implementations live here:
    // asset repository, work order repository, schedule repository,
    // latest-point cache, recent event store, metadata/profile repository.
    internal static class RepositoriesPlaceholder { }
}
